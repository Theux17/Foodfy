{%extends "layout.njk"%}

{% block head %}
    <title>Receita - {{recipe.title}}</title>
{% endblock %}

{% block container %}

    {% block header %}
            
        {% include "recipesAdmin/parts/header.njk" %}
            
    {% endblock %}

    <div id="main-container">
        {% block content %}

            <div class="recipe-information">

                <div class="title-button">
                    <h2>Receita: {{recipe.title}}</h2>
                    <a href="/admin/recipes/{{ recipe.id }}/edit"><button class="button" > Editar receita </button></a>
                </div>
                
                <img src="{{recipe.image}}" alt="{{recipe.title}}">

                <div class="recipe-name">
                    <h2>{{recipe.title}}</h2>
                </div>

                <div class="recipe-creator">
                    <span>por {{recipe.chef_name}}</span>
                </div>

                <div class="ingredients">
                    <div class="title-button">
                        <h3>Ingredientes</h3>
                    </div>

                    
                    <ul>
                        {% for ingredient in recipe.ingredients %}
                            <li class="type">- <span>{{ ingredient }}</span></li>
                        {%endfor%}
                    </ul>
                </div>

                <div class=" preparation">
                    <div class="title-button">
                        <h3>Modo de preparo</h3>
                    </div>

                    <ul >
                        {% for preparation in recipe.preparation %}
                            <li class="type">- <span>{{preparation}}</span></li>
                        {%endfor%}
                    </ul>
                </div>

                <div class="additional-information">
                    <div class="title-button">
                        <h3>Informações adicionais</h3>
                    </div>

                    <div class="text">
                        <p>{{recipe.information}}</p>
                    </div>
                </div>
            </div>
        {% endblock %}
    </div>
{% endblock %}
