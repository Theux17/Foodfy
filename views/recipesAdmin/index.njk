{%extends "layout.njk"%}

{% block head %}
    <title>Admin-Foodfy</title>
{% endblock %}

{% block header %}

<div class="container-header">
    
    <header class="header-admin">
       <div class="logo">
            <a href="/"><img src="/images/logo-admin.png " alt="Logo"></a>
        </div>
    </header>

</div>
    

{% endblock %}

{% block content %}

    <h2 class="title-admin">Gerenciar receitas</h2>
    <section class="recipes-admin">
    
            {% for recipe in recipes %}
            <div class="recipe-admin" id="{{loop.index0}}">
        
                <img src="{{ recipe.img_recipe }}" alt="{{recipe.title}}">

                <div class="recipe-content">
                    <p class="title">{{recipe.title}}</p>
                    <p class="author">por {{recipe.author}}</p>
                    <span><a href="/admin/recipes/{{ loop.index0 }}">Visualizar</a></span>
                </div>
                
            </div>
        {% endfor %}
    </section>

{% endblock %}